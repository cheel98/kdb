# LangChain and Vector Store
langchain
langchain-community
dashscope
chromadb
# 使用CPU版本的faiss，适合无GPU环境
faiss-cpu>=1.8.0,<1.9.0
numpy<2.0.0
tiktoken
python-dotenv

# Document Processing
python-docx
PyPDF2
pdfplumber
# 移除local-inference选项，减少对GPU的依赖
unstructured
# 根据操作系统选择合适的python-magic包
python-magic; platform_system != "Windows"
python-magic-bin; platform_system == "Windows"
markdown

# Web UI
streamlit

# gRPC dependencies
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# Web proxy dependencies
flask>=2.0.0
flask-cors>=3.0.0

# Database dependencies
mysqlclient>=2.1.0
SQLAlchemy>=2.0.0
pymysql>=1.0.0
cryptography>=40.0.0